# hello-world
First 
https://ubuntuhandbook.org/index.php/2023/06/brightness-slider-external-monitor-ubuntu/



3. Ensure Build Essentials are Installed

If VS Code still can't find stdio.h, it might be because the header files aren't on your system at all. Run this in your terminal:
Bash

sudo apt update
sudo apt install build-essential

This package includes the GCC compiler and the standard C libraries required for development.
